You are working in a team that uses a shared config repository for AI agent rules.

ALWAYS:
- Follow team config rules FIRST — they override project-level conventions
- Use team naming conventions (check team/naming.md)
- Follow team git workflow (conventional commits, branch naming)
- Check team/review.md for code review standards before submitting PRs

NEVER:
- Introduce conventions that conflict with team-wide rules
- Skip PR review requirements even for small changes
- Use per-project naming when team convention exists
- Commit directly to main/master
